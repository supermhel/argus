# <WS-N> <Service Name> — Interface Declaration

> Filled in by the workstream. Declares the contract surface only.

## Consumes
- Topics: `<topic>` (group `<cg-...>`)
- Contracts: `<A|B|C|D|E>`
- HTTP: `<endpoint>` (if any)

## Produces
- Topics: `<topic>` (partition key `<key>`)
- HTTP: `<endpoint>` (if any)

## Mocks provided
- `<what this WS stubs so neighbours can develop independently>`

## Contract tests
- `<command to run>`

## Run locally
- `<command>`
