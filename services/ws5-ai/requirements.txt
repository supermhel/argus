# WS-5 AI. Core skeleton is stdlib-only (StubLLM + rule-of-thumb classifier).
# Production extras:
#   scikit-learn>=1.3   # real layer-2 classifier (TF-IDF + logreg)
#   redis>=5.0          # bus backend
# Local LLM runtime: Ollama (external service), set OLLAMA_URL + OLLAMA_MODEL.
